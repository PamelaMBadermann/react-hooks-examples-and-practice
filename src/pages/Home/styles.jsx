import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  blue: {
    background: 'blue',
    color: 'white'
  },
  red: {
    background: 'red',
    color: 'white'
  }
}));
export default useStyles;